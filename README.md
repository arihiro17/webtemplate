# Web Template

## インストール
`npm install` or `yarn install`


## 使い方

### 開発開始
`npm run start` or `yarn start`  
`localhost:8888` でプレビュー

### ビルド
`npm run start` or `yarn start`  
`dist` フォルダに納品ファイルが出力される
